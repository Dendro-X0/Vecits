# CodaCtrl Studio Output Hub

Indexable studio outputs for this repository. Internal daemon state lives under `.codectx/`.

- Generated: 2026-07-03T06:24:03.277049300+00:00
- Entries: 6

## Modules

### Technical survey (`technical`)
- Module readme: [`.codactrl/technical/README.md`](.codactrl/technical/README.md)
- Entries: 6

## Machine index

See [`index.json`](index.json) for the full entry catalog.

## Git

`.codectx/` holds daemon machine state (typically gitignored). `.codactrl/` is the agent navigation hub.
`.codactrl/.gitignore` defaults to ignoring synced mirrors. Edit it to commit curated outputs; use `git add -f` for one-off adds.
Verify lane: see [`.codactrl/verify/README.md`](verify/README.md) for per-pass commit guidance.
