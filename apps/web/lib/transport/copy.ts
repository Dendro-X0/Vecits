/** Mandatory kernel-truth copy on all R8 Tier 0 share panels (spec §3 Tier 0). */
export const TRANSPORT_QR_WARNING =
  "Scanning does not publish offers or move credits. Confirm the destination before acting.";

/** R9-N1 NFC import copy (offline-transport-spec §4.3). */
export const TRANSPORT_NFC_WARNING =
  "Tapping does not publish offers or move credits. Confirm node URL before connecting.";

/** Tier 1 bundle review copy (spec §3 Tier 1). */
export const TRANSPORT_NODE_CONFIRM_WARNING = "Confirm node URL before connecting.";

export const TRANSPORT_BUNDLE_EXPIRED_MESSAGE =
  "Link expired — ask sender to regenerate.";
