# Copy is optional — `pnpm dev:web` runs scripts/ensure-dev-web-port.mjs automatically.
# Assigned port is persisted in repo-root `.dev/web-port` (gitignored).

PORT=4187

# Kernel API (unchanged unless you run a second node on another port)
# NEXT_PUBLIC_NODE_API_BASE_URL=http://127.0.0.1:7878
