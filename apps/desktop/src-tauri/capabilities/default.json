{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Vectis shell",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
